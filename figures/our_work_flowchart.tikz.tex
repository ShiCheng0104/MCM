% Our Work flowchart (TikZ) — compact 2×2 task panels + shared input/output
% Usage: \input{../figures/our_work_flowchart.tikz.tex}

\begingroup
\definecolor{DeepNavy}{HTML}{264653}
\definecolor{Teal}{HTML}{2A9D8E}
\definecolor{Sand}{HTML}{E9C46B}
\definecolor{Orange}{HTML}{F3A261}
\definecolor{Coral}{HTML}{E86F52}

\begin{tikzpicture}[
  font=\small,
  node distance=6mm,
  >={Stealth[length=2.2mm,width=2.2mm]},
  box/.style={rounded corners=2.2mm, draw=DeepNavy, line width=0.6pt, align=left, inner xsep=6pt, inner ysep=6pt},
  panel/.style={box, minimum height=23mm},
  title/.style={font=\bfseries, text=DeepNavy},
  arrow/.style={-Stealth, line width=0.6pt, draw=DeepNavy},
  dashedarrow/.style={-Stealth, line width=0.6pt, draw=DeepNavy, dashed},
  ministep/.style={rounded corners=1.8mm, draw=DeepNavy!70, fill=white, line width=0.4pt, align=left, inner xsep=4pt, inner ysep=3.5pt}
]

% Shared Inputs (top)
\node[box, fill=Teal!15, text width=0.92\linewidth] (A) {
  {\title{Shared Inputs (Data Hub)}}\\
  Judges’ scores + eliminations (weekly)\\
  Contestant attributes (age / industry / partner)\\
  External signals: Google Trends + TV ratings\\
  Cleaning & alignment: names, weeks, missing seasons
};

% Task panels (2×2 grid)
\node[panel, fill=Sand!25, text width=0.44\linewidth, below left=9mm and 0mm of A.south] (T1) {\title{Task 1: Hidden Audience Vote Inference}};
\node[panel, fill=Orange!20, text width=0.44\linewidth, below right=9mm and 0mm of A.south] (T2) {\title{Task 2: Rule Comparison \& Controversy}};
\node[panel, fill=Orange!18, text width=0.44\linewidth, below=10mm of T1.south] (T3) {\title{Task 3: Driver Analysis (Judges vs Audience)}};
\node[panel, fill=Coral!18, text width=0.44\linewidth, below=10mm of T2.south] (T4) {\title{Task 4: New System Design + Backtesting}};

% Mini-steps inside T1
\node[ministep, fill=white, text width=0.41\linewidth, anchor=north west] (t1a) at ([xshift=6pt,yshift=-18pt]T1.north west) {
  \textbf{Prior}: performance–popularity fusion\\
  \scriptsize $\hat{V}_i\propto S_i^{\alpha}(1+\theta_i)(1+w_pp_i)(1+w_rr_i)$
};
\node[ministep, fill=white, text width=0.41\linewidth, below=3.5mm of t1a] (t1b) {
  \textbf{Posterior}: match eliminations\\
  SoftRank + SLSQP (regularized)
};
\node[ministep, fill=white, text width=0.41\linewidth, below=3.5mm of t1b] (t1c) {
  \textbf{Uncertainty}: Monte Carlo CI\\
  CV stability score
};

% Mini-steps inside T2
\node[ministep, fill=white, text width=0.41\linewidth, anchor=north west] (t2a) at ([xshift=6pt,yshift=-18pt]T2.north west) {
  Counterfactual simulator\\
  Rank vs Percent outcomes
};
\node[ministep, fill=white, text width=0.41\linewidth, below=3.5mm of t2a] (t2b) {
  Disagreement \& bias diagnostics\\
  “More audience-favoring?”
};
\node[ministep, fill=white, text width=0.41\linewidth, below=3.5mm of t2b] (t2c) {
  Controversy spotlight (4 cases)\\
  trajectories + what-if
};

% Mini-steps inside T3
\node[ministep, fill=white, text width=0.41\linewidth, anchor=north west] (t3a) at ([xshift=6pt,yshift=-18pt]T3.north west) {
  Regression / mixed effects\\
  marginal impacts
};
\node[ministep, fill=white, text width=0.41\linewidth, below=3.5mm of t3a] (t3b) {
  Random forest importance\\
  (optional) SHAP-style explanations
};

% Mini-steps inside T4
\node[ministep, fill=white, text width=0.41\linewidth, anchor=north west] (t4a) at ([xshift=6pt,yshift=-18pt]T4.north west) {
  Dynamic weights by stage\\
  + early technical protection
};
\node[ministep, fill=white, text width=0.41\linewidth, below=3.5mm of t4a] (t4b) {
  Controversy trigger + judge save\\
  suspense, but controlled
};
\node[ministep, fill=white, text width=0.41\linewidth, below=3.5mm of t4b] (t4c) {
  Backtest scorecard\\
  fairness / entertainment / controversy / simplicity
};

% Shared Outputs (bottom)
\node[box, fill=Coral!22, text width=0.92\linewidth, below=11mm of T3.south, xshift=0.48\linewidth] (B) {
  {\title{Shared Outputs (Deliverables)}}\\
  Inferred votes + uncertainty (tables/figures)\\
  Evidence-based rule recommendation + tradeoffs\\
  Proposed new system + backtesting report
};

% Arrows: inputs
\draw[arrow] (A.south west) ++(0.12\linewidth,0) -- ++(0,-4mm) -| (T1.north);
\draw[arrow] (A.south east) ++(-0.12\linewidth,0) -- ++(0,-4mm) -| (T2.north);
\draw[arrow] (A.south) -- ++(0,-3mm) -| (T3.north);

% Couplings
\draw[arrow] (T1.east) -- node[above, font=\scriptsize, text=DeepNavy]{inferred votes} (T2.west);
\draw[arrow] (T1.south) -- node[left, font=\scriptsize, text=DeepNavy]{inferred votes} (T3.north);
\draw[arrow] (T1.south east) ++(-2mm,0) -- ++(0,-5mm) -| node[pos=0.75, font=\scriptsize, text=DeepNavy]{votes + uncertainty} (T4.north);
\draw[arrow] (T2.south) -- ++(0,-5mm) -| node[pos=0.75, font=\scriptsize, text=DeepNavy]{what works / fails} (T4.north);
\draw[arrow] (T3.east) -- ++(6mm,0) |- node[pos=0.25, font=\scriptsize, text=DeepNavy]{drivers → design knobs} (T4.west);

% Outputs
\draw[arrow] (T2.south) ++(0,-2mm) -- ++(0,-6mm) -| (B.north);
\draw[arrow] (T3.south) ++(0,-2mm) -- ++(0,-6mm) -| (B.north);
\draw[arrow] (T4.south) -- ++(0,-5mm) |- (B.north);

% One feedback loop (dashed)
\draw[dashedarrow] (T4.north) ++(0,0) .. controls +(0,10mm) and +(16mm,-6mm) .. node[above, font=\scriptsize, text=DeepNavy]{iterate \& recalibrate} (T1.north east);

\end{tikzpicture}
\endgroup
